apiVersion: v1
kind: Service
metadata:
  name: weather-prediction-server
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
  selector:
    app: weather-prediction-server
